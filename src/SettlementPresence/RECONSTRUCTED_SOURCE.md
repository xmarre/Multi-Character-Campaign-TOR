# Reconstructed source — SettlementPresence

These C# files were reconstructed from the exact Multi-Character Campaign - TOR v1.0.41 shipped binary for this project using ILSpyCmd 11.0.0.9225 with C# 7.3 output.

They are **not** the lost original authoring source. The exact v1.0.41 execution baseline remains the matching files under `../../CanonicalIL/SettlementPresence/` and the authoritative DLL shipped in `../../../bin/Win64_Shipping_Client/`.

Future development may use this C# tree, but any ambiguous reconstruction should be checked against `Authoritative.ikdasm.il` before changing behavior.
