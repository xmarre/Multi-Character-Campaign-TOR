# RuntimeCompatibility source recovery

`CareerAbilityRepair.cs` contains the first 815 lines recovered from the staged repository archive. The archive ended at the opening brace of `GetStringId`.

`CareerAbilityRepair.tail.csfrag` contains the exact remaining 39 lines from the uploaded v1.0.41 reconstructed source package. `MultiCharacterCampaignTOR.RuntimeCompatibility.v140.csproj` joins both fragments into `obj/Release/RecoveredCareerAbilityRepair.cs` before compilation.

This split is a source-transport recovery detail. It does not alter the compiled v1.0.41 runtime-compatibility logic.
