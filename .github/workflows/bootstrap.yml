name: Bootstrap repository and validate

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  bootstrap-and-build:
    runs-on: windows-latest
    steps:
      - run: Write-Host "Bootstrap payload upload in progress"
